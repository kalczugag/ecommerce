import type { Order } from "@/types/Order";

interface AddressesPageProps {
    data: Order;
}

const AddressesPage = ({ data }: AddressesPageProps) => {
    return <div>addresses</div>;
};

export default AddressesPage;
