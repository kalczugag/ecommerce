import type { Order } from "@/types/Order";

interface ReturnsPageProps {
    data: Order;
}

const ReturnsPage = ({ data }: ReturnsPageProps) => {
    return <div>returns</div>;
};

export default ReturnsPage;
