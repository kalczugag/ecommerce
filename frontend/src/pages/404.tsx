import DefaultLayout from "@/layouts/DefaultLayout";

const NotFound = () => {
    return (
        <DefaultLayout>
            <h1>Page not found</h1>
        </DefaultLayout>
    );
};

export default NotFound;
