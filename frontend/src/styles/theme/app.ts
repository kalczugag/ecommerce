import { createTheme } from "@mui/material/styles";

const appTheme = createTheme({
    typography: {
        button: {
            textTransform: "none",
        },
    },
});

export default appTheme;
